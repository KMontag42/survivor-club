<%= form_for(@admin_castaway, layout: :basic) do |f| %>
  <% if @admin_castaway.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_castaway.errors.count, "error") %> prohibited this admin_castaway from being saved:</h2>

      <ul>
      <% @admin_castaway.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
      <%= f.text_field :name %>
      <%= f.select :season_id, Season.all.collect{|x| [x.name, x.id]} %>
      <%= f.select :original_tribe_id, Tribe.all.collect{|x| [x.name, x.id]} %>
      <%= f.select :swapped_tribe_id, Tribe.all.collect{|x| [x.name, x.id]} %>
      <%= f.select :merged_tribe_id, Tribe.all.collect{|x| [x.name, x.id]} %>
      <%= f.select :voted_out_episode_id, Episode.all.collect{|x| [x.name, x.id]} %>
    <%= f.submit %>
  </div>
<% end %>
