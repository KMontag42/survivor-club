<%= form_for(@admin_season) do |f| %>
  <% if @admin_season.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_season.errors.count, "error") %> prohibited this admin_season from being saved:</h2>

      <ul>
      <% @admin_season.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
