<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
  <title>Survivor Club</title>
  <%= Gon::Base.render_data({init: true}) %>
  <link href="http://js-grid.com/css/jsgrid.min.css" rel="stylesheet" />
  <link href="http://js-grid.com/css/jsgrid-theme.min.css" rel="stylesheet" />
  <link rel="apple-touch-startup-image" href="//fillmurray.com/320/480">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black">
  <link rel="apple-touch-icon" href="//fillmurray.com/60/60">
  <%= stylesheet_link_tag font_awesome_css(version: '4.4.0') %>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <script src="http://js-grid.com/js/jsgrid.min.js"></script>
  <%= csrf_meta_tags %>
  <script type="text/javascript">
    App.globals.user.email = '<%= current_user.try(:email) %>';
    App.globals.user.name = '<%= current_user.try(:name) %>';
    App.globals.user.id = '<%= current_user.try(:id) %>';
  </script>
</head>
<body id="<%= yield :body_id %>"
      class="<%= yield :body_class %>"
      style="<%= yield :body_style %>">
<div class="">
    <%= render partial: 'layouts/navbar' %>

    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-10 col-sm-offset-1 col-xs-12">
                <!-- flash notifications -->
                <% flash.each do |key, value| %>
                  <%= alert_box value, dismissible: true, context: key %>
                <% end %>
                <div class="alert alert-success">
                  <h1>THE KAOH RONG DRAFT IS HERE</h1>
                  <h2><%= link_to draft_path(14) %></h2>
                </div>
                <%= yield %>
            </div>
        </div>
    </div>
</div>
<!--<div class="visible-xs-block">-->
    <!--<h1>-->
        <!--Check out the mobile beta page here:-->
        <!--<a href="//www.thekylemontag.com/suvivor-club-mobile">SC Mobile</a>-->
    <!--</h1>-->
<!--</div>-->
</body>
</html>
